import { RegisterOptions } from "react-hook-form"

export default {
  maxLength: {
    value: 14,
    message: "max length is 14",
  },
  minLength: {
    value: 4,
    message: "min length is 4",
  },
} as RegisterOptions
